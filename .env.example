# Example values; replace with real secrets locally and in Vercel env config.
# Prisma Accelerate/Data Proxy sample (replace with your accelerate URL):
# DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=..."
DATABASE_URL="postgresql://postgres:password@localhost:5432/enabion_bcos"
APP_ENV="dev"
